mixin passengerCard(data) 
    .passengerCard.text-center
        p #{data}

mixin field(type, name, placeholder, value)
  .field
    .ui.left.icon.input
      i.icon(class=(type === 'password' ? 'lock' : 'user'))
      input(type=type, name=name, placeholder=placeholder, required=true, value=value)
